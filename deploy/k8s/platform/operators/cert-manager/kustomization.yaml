# cert-manager, vendored — the same shape ADR-0099 decision 2 gave CloudNativePG.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cert-manager-v1.21.2.yaml
  - gateway-api-rbac.yaml

patches:
  - path: patch-gateway-api.yaml
