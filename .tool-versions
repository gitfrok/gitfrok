# Super-repo union of host toolchain floors (ADR-0089). Each code submodule also carries its own.
golang 1.27.0
nodejs 26.7.0
# TypeScript via npm (floor 7). git >= 2. Service image tags: deploy/dev/versions.env.
