apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agents-gitfrok
spec:
  parentRefs: [{name: gitfrok-cp, sectionName: app-https}]
  rules:
    - backendRefs: [{name: controlplane-agent-door, port: 443}]
